interface:
  display_name: "Database Schema Designer"
  short_description: "Design schemas with indexes and migrations."
  default_prompt: "Use $database-schema-designer to design a production-ready database schema for my workload; clarify critical gaps, then produce tables or collections, indexes, migrations, query examples, and validation checks."
